#include "Header.h"

int main()
{
    unsigned int n;  //кількість слів у масиві слів
    Word* Arr = Array_Create(n);  //створення масиву слів
    Output(Arr,n);  //виведення масиву слів
    WordWithMostDigits(Arr, n);   //знаходження у масиві слова з найбільшою кількістю цифр
    MemClear(Arr);  //очищення пам'яті, виділеної для динамічного масива Arr
    return 0;
}
// Розробити клас "слово". Створити масив об'єктів даного класу. Визначити слово з найбільшою кількістю цифр.